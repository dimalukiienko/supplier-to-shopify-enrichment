# Evals

Promptfoo-based offline evaluation of prompts and graph behavior against the
sample dataset (`data/`). See `docs/ARCHITECTURE.md` §7.

```bash
npx promptfoo@latest eval -c evals/promptfooconfig.yaml
```
