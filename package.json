{
  "name": "supplier-to-shopify-enrichment",
  "version": "0.0.0",
  "private": true,
  "description": "Supplier-to-Shopify enrichment workflow monorepo",
  "packageManager": "pnpm@11.0.9",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "test": "turbo run test",
    "gen:types": "turbo run gen:types",
    "db:migrate": "supabase db push"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "supabase": "^2.106.0",
    "turbo": "^2.5.0",
    "typescript": "^5.6.3"
  }
}
